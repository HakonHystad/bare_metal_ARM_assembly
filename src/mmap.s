	// hold the memory layout of the broadcom2836

	.equ GPIOaddr, 0x3F200000	// the GPIO base address
	.equ timerAddr, 0x3F003000	// timer base address
	.equ mailboxAddr, 0x3F00B880	// mailbox base address
